# Server Configuration
# 服务器端口号 (默认: 3000)
PORT=3000

# Bilibili SESSDATA cookie (可选)
# 用于获取更高清晰度的视频流
# 从浏览器登录 Bilibili 后获取
SESSDATA=

# CORS 配置
# 允许跨域访问的源 (默认: *)
CORS_ORIGIN=*

# ============================================
# Video to Markdown 功能配置
# ============================================
# 以下配置用于将 Bilibili 视频转换为 Markdown 文档
# 包括视频下载、OSS 上传、通义听悟 AI 转录等功能
#
# 【重要】前端配置与环境变量的关系：
# 1. 环境变量配置（本文件）：
#    - 适用于服务器端默认配置
#    - 当前端未提供配置时作为后备选项
#    - 适合多用户共享的默认设置
#
# 2. 前端配置（浏览器界面）：
#    - 通过 Web 界面的"设置"按钮访问
#    - 配置存储在浏览器 Local Storage 中
#    - 优先级高于环境变量
#    - 适合个人用户的自定义配置
#
# 3. 配置优先级：
#    前端配置 > 环境变量配置
#    即：如果前端提供了配置，将使用前端配置；
#        如果前端未提供配置，将使用环境变量配置
#
# 4. 使用建议：
#    - 首次使用：在本文件配置环境变量，系统会自动读取并填充到前端
#    - 个人定制：在前端界面修改配置，无需修改本文件
#    - 团队共享：在本文件配置默认值，团队成员可在前端覆盖
#
# 详细文档：https://github.com/your-repo/wiki/Configuration-Guide

# --------------------------------------------
# 通义听悟配置
# --------------------------------------------
# TINGWU_APP_KEY (必需)
# 用于 AI 语音转文字和 PPT 提取
# 获取方式: 登录阿里云通义听悟控制台 (https://tingwu.aliyun.com)
# 创建应用后获取 App Key
# 
# 前端配置：可在 Web 界面的"设置"中配置此项
# 字段名称：通义听悟 App Key
# Validates: Requirement 9.1
TINGWU_APP_KEY=

# --------------------------------------------
# 阿里云访问密钥配置
# --------------------------------------------
# ALIYUN_ACCESS_KEY_ID (必需)
# 阿里云 Access Key ID，用于 OSS 上传和通义听悟 API 认证
# 获取方式: 登录阿里云 RAM 控制台 (https://ram.console.aliyun.com)
# 创建 AccessKey 或使用已有的 AccessKey
# 
# 前端配置：可在 Web 界面的"设置"中配置此项
# 字段名称：阿里云 Access Key ID
# Validates: Requirement 9.2
ALIYUN_ACCESS_KEY_ID=

# ALIYUN_ACCESS_KEY_SECRET (必需)
# 阿里云 Access Key Secret，与 Access Key ID 配对使用
# 注意: 请妥善保管，不要泄露或提交到代码仓库
# 
# 前端配置：可在 Web 界面的"设置"中配置此项
# 字段名称：阿里云 Access Key Secret
# 安全提示：前端配置时，此字段默认为密码样式（隐藏显示）
# Validates: Requirement 9.3
ALIYUN_ACCESS_KEY_SECRET=

# --------------------------------------------
# 阿里云 OSS 配置
# --------------------------------------------
# OSS_BUCKET (必需)
# OSS 存储桶名称，用于存储上传的视频文件
# 获取方式: 登录阿里云 OSS 控制台 (https://oss.console.aliyun.com)
# 创建 Bucket 或使用已有的 Bucket
# 注意: Bucket 需要开启公共读权限以便通义听悟访问
# 
# 前端配置：可在 Web 界面的"设置"中配置此项
# 字段名称：OSS Bucket
# Validates: Requirement 9.4
OSS_BUCKET=

# OSS_REGION (可选)
# OSS 区域标识符
# 默认值: oss-cn-hangzhou (杭州)
# 常用区域:
#   - oss-cn-hangzhou (杭州)
#   - oss-cn-shanghai (上海)
#   - oss-cn-beijing (北京)
#   - oss-cn-shenzhen (深圳)
# 获取方式: 在 OSS 控制台查看 Bucket 所在区域
# 
# 前端配置：可在 Web 界面的"设置"中配置此项
# 字段名称：OSS Region
# 提示：前端界面会显示默认值提示
# Validates: Requirement 9.5
OSS_REGION=oss-cn-hangzhou

# --------------------------------------------
# Jina Reader 网页抓取配置（用于“网页链接生成笔记”）
# --------------------------------------------
# JINA_READER_ENDPOINT (可选)
# 默认值: https://r.jina.ai/
JINA_READER_ENDPOINT=https://r.jina.ai/

# JINA_API_KEY / JINA_READER_API_KEY (可选)
# 有 API Key 可获得更高限额
JINA_API_KEY=

# JINA_READER_TIMEOUT_SEC (可选)
# 请求超时秒数，建议范围 3-180
JINA_READER_TIMEOUT_SEC=30

# JINA_READER_NO_CACHE (可选)
# true/1/yes 表示每次抓取绕过缓存
JINA_READER_NO_CACHE=false


# ============================================
# 配置管理说明
# ============================================

# 【前端配置界面使用指南】
# 
# 1. 访问配置界面：
#    - 打开 Web 应用 (http://localhost:3000)
#    - 点击页面右上角的"设置"按钮
#    - 在弹出的配置模态框中输入配置
#
# 2. 配置字段说明：
#    - AI API URL: AI 服务的 API 端点（如 OpenAI、Claude 等）
#    - API Key: AI 服务的认证密钥（自动隐藏显示）
#    - 整理提示词: 自定义 AI 整理笔记的提示词（可隐藏显示）
#    - 通义听悟 App Key: 阿里云通义听悟应用密钥
#    - 阿里云 Access Key ID: 阿里云访问密钥 ID
#    - 阿里云 Access Key Secret: 阿里云访问密钥（自动隐藏显示）
#    - OSS Bucket: 阿里云 OSS 存储桶名称
#    - OSS Region: 阿里云 OSS 区域（可选，默认 oss-cn-hangzhou）
#
# 3. 配置功能：
#    - 实时验证：输入时自动验证配置格式
#    - 测试连接：保存前可测试 AI API 连接是否正常
#    - 显示/隐藏：敏感字段（API Key、Secret）支持切换显示
#    - 自动填充：首次打开时自动读取环境变量配置
#    - 本地存储：配置保存在浏览器 Local Storage 中
#
# 4. 配置获取方式：
#    - 通义听悟 App Key: https://tingwu.aliyun.com
#    - 阿里云 Access Key: https://ram.console.aliyun.com
#    - OSS Bucket: https://oss.console.aliyun.com
#    - AI API Key: 
#      * OpenAI: https://platform.openai.com/api-keys
#      * Claude: https://console.anthropic.com/settings/keys
#      * 其他 AI 服务请参考对应文档
#
# 5. 安全提示：
#    - 前端配置存储在浏览器本地，仅当前浏览器可访问
#    - 敏感字段默认隐藏显示，可通过眼睛图标切换
#    - 不要在公共计算机上保存敏感配置
#    - 定期更新 API Key 和 Access Key
#
# 6. 故障排除：
#    - 如果配置未生效，请检查浏览器控制台是否有错误
#    - 如果自动填充失败，请手动输入配置
#    - 如果测试连接失败，请检查 API URL 和 Key 是否正确
#    - 清除浏览器缓存可重置前端配置
#
# 详细文档和常见问题：
# https://github.com/your-repo/wiki/Configuration-Guide
# 或查看项目根目录下的 CONFIGURATION_GUIDE_CH.md 文件
