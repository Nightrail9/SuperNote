services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bilibili-video-parser
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3000
    volumes:
      - ./storage/data:/app/storage/data
      - ./storage/temp:/app/storage/temp
    restart: unless-stopped
